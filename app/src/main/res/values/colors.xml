<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="ic_launcher_background">#1A237E</color>
    <!-- Dark scrim behind the cast intro overlay: its text is white, so the
         default (near-transparent on a light theme) makes it unreadable. -->
</resources>
